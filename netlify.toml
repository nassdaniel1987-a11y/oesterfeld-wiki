[build]
  command = "rm -f quartz.lock.json && npx quartz plugin install --from-config && npx quartz build"
  publish = "public"
  functions = "netlify/functions"

[build.environment]
  NODE_VERSION = "22"
